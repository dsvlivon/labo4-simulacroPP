export class Actor {
    id: string;
    nombre: string;
    pais: string;

    constructor(
        id: string,
        nombre: string,
        pais: string
    ) {
        this.id = id;
        this.nombre = nombre;
        this.pais = pais;
    }
}
